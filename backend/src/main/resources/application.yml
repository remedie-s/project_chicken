spring:
  application:
    name: backend
  kafka:
    consumer:
      bootstrap-servers: localhost:9092  # Kafka 클러스터 주소
      group-id: your-consumer-group  # 소비자 그룹
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      bootstrap-servers: host.docker.internal:9092  # Kafka 클러스터 주소
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-project.clskqmqggip6.ap-northeast-2.rds.amazonaws.com:3306/project
    username: admin
    password: 1q2w3e4r
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
    jpa:
      database-platform: org.hibernate.dialect.MySQL8Dialect
      hibernate:
        ddl-auto: update  # 개발 환경에서는 주로 update 사용
      #show-sql: true  # SQL 쿼리 출력
      properties:
        hibernate:
          format_sql:true