import {AppBar, Box} from "@mui/material";

export default function BrandTab(){
    return(
        <Box>
            <AppBar sx={{}}>
                <Box></Box>
            </AppBar>
        </Box>)
}